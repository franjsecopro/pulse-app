fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
pydantic[email]==2.9.0
pydantic-settings==2.5.0
python-jose[cryptography]==3.3.0
bcrypt==4.2.0
python-multipart==0.0.12
alembic==1.13.3
asyncpg==0.29.0
greenlet==3.1.1
openpyxl==3.1.5
slowapi==0.1.9
cryptography==46.0.6
httpx==0.28.1
jinja2==3.1.6
# Invoice PDF rendering. Needs native GTK libs — use WSL/Docker on Windows dev;
# runs natively on Linux/prod. Imported lazily in invoice_pdf_service.
weasyprint==69.0
# Timezone database for zoneinfo (Europe/Paris business day in scheduled jobs).
# Required on Windows and minimal Linux images that lack a system tz database.
tzdata==2026.1
